# dependencies — reinstalled from package-lock.json with `npm ci`
node_modules/

# build output
dist/
dist-ssr/
*.local

# personal financial data — never push these
*.pdf
*.csv

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment / secrets (ANTHROPIC_API_KEY lives here once the proxy exists)
.env
.env.*
!.env.example

# editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
